name: Release build
run-name: >
  Release job triggered by 
  ${{ github.event_name }} on 
  ${{ github.event.pull_request.number || format('{0}/merge', github.event.issue.number) }}

on:
  pull_request:
    branches: [master]
    types: [opened, labeled, unlabeled, closed]
  issue_comment:
    types: [created]

jobs:
  validate-for-release:
    if: |
      (
        github.event_name == 'pull_request' &&
          (
            github.event.action == 'opened' ||
            github.event.action == 'labeled' ||
            github.event.action == 'unlabeled'
          )
      ) || (
        github.event_name == 'issue_comment' &&
        contains(
          github.event.comment.body, '/validate-for-release'
        )
      )
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - name: Debug event payload
        run: |
          echo "event name: ${{ github.event_name }}"
          echo "pull_request.number: ${{ github.event.pull_request.number }}"
          echo "issue.number: ${{ github.event.issue.number }}"
          echo "issue.pull_request exists: ${{ github.event.issue.pull_request }}"
        id: debug
      - name: Checkout
        uses: actions/checkout@v5
        id: checkout
      - name: Validate for release
        uses: ./
        id: validate_for_release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pull_request_id: ${{ github.event.pull_request.number || github.event.issue.number }}
          mode: validate

  create-release:
    if: |
      github.event_name == 'pull_request' && 
      github.event.action == 'closed' && 
      github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        id: checkout
      - name: Create release
        uses: ./
        id: create_release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pull_request_id: ${{ github.event.pull_request.number }}
          mode: release
